- id: css-file-stage-shell
  title: Add CSS for .ds-file-stage layout container (padding, max-width, column flow) in app-shell.css — currently unstyled, fsbrowse main content has no wrapper styling
  witness: 'browser-6: .ds-file-stage computed max-width=920px display=flex (centered column). app-shell.css ds-file-stage block.'
  status: completed
- id: css-dropzone
  title: Add CSS for .ds-dropzone / .ds-dropzone-inner / .ds-dropzone-glyph / .ds-dropzone-label including .dragover active state (dashed border, accent highlight) — fully unstyled now
  witness: 'browser-6: .ds-dropzone computed border-style=dashed border-radius=22px. dragover pulse + accent-tint in app-shell.css.'
  status: completed
- id: css-modal-backdrop
  title: Add CSS for .ds-modal-backdrop / .ds-modal / .ds-modal-small / .ds-modal-preview / .ds-modal-head / .ds-modal-body / .ds-modal-actions / .ds-modal-input — backdrop blur, centered card, z-index over --z-modal; modals render unstyled
  witness: 'browser-6: .ds-modal-backdrop position=fixed z-index=800; .ds-modal radius=22px max-width=880px (preview). Modal opened on click, rendered centered card.'
  status: completed
- id: css-preview
  title: Add CSS for .ds-preview-media / .ds-preview-audio / .ds-preview-code / .ds-preview-text / .ds-preview-fallback / .ds-preview-glyph / .ds-preview-head / .ds-preview-name / .ds-preview-meta / .ds-preview-actions / .ds-preview-body / .ds-preview-truncated — file viewer body unstyled
  witness: 'browser-6: .ds-preview-code background=rgb(19,19,24) overflow-y=auto; preview-head/name/meta/body/media all in app-shell.css. Code preview rendered in opened modal.'
  status: completed
- id: css-upload-progress
  title: Add CSS for .ds-upload-progress / .ds-upload-item / .ds-upload-name / .ds-upload-bar / .ds-upload-fill / .ds-upload-pct incl. done/error states and data-pct width driving — progress bar invisible now
  witness: app-shell.css .ds-upload-progress/.ds-upload-item (grid name|bar|pct) /.ds-upload-bar (6px pill track) /.ds-upload-fill (accent, width transition) /.ds-upload-pct + done(success)/error(warn) state colors. In dist bundle (grep ds-upload-fill confirmed). Width driven by inline data-pct->style by fsbrowse app at runtime.
  status: completed
- id: css-crumb-path
  title: Add CSS for .ds-crumb-path / .ds-crumb-seg / .ds-crumb-seg.leaf / .ds-crumb-sep — breadcrumb buttons render as bare unstyled buttons
  witness: 'browser-6: .ds-crumb-seg padding-left=8px border-radius=10px cursor=pointer (styled button, not bare).'
  status: completed
- id: css-file-toolbar
  title: Add CSS for .ds-file-toolbar / .ds-file-toolbar-left / .ds-file-toolbar-right (flex row, space-between, gap) — toolbar buttons unaligned
  witness: 'browser-6: .ds-file-toolbar display=flex justify-content=space-between.'
  status: completed
- id: css-file-row-internals
  title: 'Fix .ds-file-row internal CSS: markup renders .ds-file-icon, .title, .ds-file-meta, .ds-file-actions, .ds-file-act but CSS targets stale .name/.size/.time. Style icon sizing, title truncation (ellipsis), meta mono color, action buttons (hover reveal, warn color for delete)'
  witness: 'browser-6: .ds-file-row display=grid border-left-width=2.4px (type accent). app-shell.css now targets .title/.ds-file-meta/.ds-file-icon/.ds-file-actions/.ds-file-act (rendered classes), stale .name/.size/.time removed. Title ellipsis + hover-reveal actions + warn delete added.'
  status: completed
- id: css-file-empty
  title: Add CSS for .ds-file-empty / .ds-file-empty-glyph / .ds-file-empty-text — empty directory state unstyled
  witness: app-shell.css .ds-file-empty/.ds-file-empty-glyph/.ds-file-empty-text block added (centered column, muted). Built into dist (grep confirmed).
  status: completed
- id: css-error-banner-meta-mono
  title: Add CSS for .ds-error-banner (fsbrowse error toast, dismissible, danger color) and .ds-meta-mono (item count chip in toolbar right)
  witness: 'browser-6: .ds-meta-mono present (toolbar right). app-shell.css .ds-error-banner (warn-tinted dismissible) + .ds-meta-mono (mono caps) added; in dist bundle.'
  status: completed
- id: theme-dark-token-mismatch
  title: 'fsbrowse index.html sets data-theme=dark|light from localStorage, but the kit only defines dark tokens under [data-theme=ink] and [data-theme=auto]. So ''dark'' yields NO dark tokens — file browser stays light in dark mode. Reconcile: either map dark->ink in kit, or have fsbrowse emit ink/auto. Decide and fix.'
  witness: 'browser-6: body backgroundColor=rgb(19,19,24)=--ink, i.e. dark tokens ARE applying. Fixed by adding [data-theme=dark] alias to the ink dark-token block in colors_and_type.css; rebuilt dist (grep data-theme=dark in dist confirmed).'
  status: completed
- id: syntax-highlighting
  title: FilePreviewCode renders <code class=language-xx> but no highlight.js is loaded in fsbrowse index.html (CLAUDE.md claims hljs CDN but index.html has none). Either wire hljs or ensure .ds-preview-code is legible mono with proper bg. Verify code preview readability.
  witness: 'browser-6: .ds-preview-code background=rgb(19,19,24) (dark) overflow-y=auto with mono font — legible code preview without hljs CDN dependency, consistent with kit''s no-CDN philosophy. Decision: CSS-only legibility, no hljs.'
  status: completed
- id: build-dist
  title: Rebuild dist/247420.css + 247420.js via node scripts/build.mjs after CSS edits so the bundle fsbrowse actually loads contains the new styles
  witness: 'node scripts/build.mjs => ''[247420] css scoped+bundled: 150.3kb''; grep ds-modal-backdrop|ds-dropzone|ds-preview-code|ds-upload-fill|ds-file-stage|data-theme=dark in dist/247420.css => 22 matches. fsbrowse served /_ds/247420.css with new rules (browser-6 computed styles applied).'
  status: completed
- id: verify-browser
  title: 'Browser-witness: launch fsbrowse against a temp dir with mixed file types, assert computed styles on .ds-dropzone, .ds-modal, .ds-file-row, .ds-upload-bar, .ds-crumb-seg are non-default (padding/border/bg applied), and dark theme applies dark bg. Required by paper s23 for client-side CSS edits.'
  witness: browser-6 (session 36, headless Chrome via plugkit) navigated to live fsbrowse, asserted computed styles on dropzone/fileRow/crumb/toolbar/stage/backdrop/modal/previewCode all non-default + dark body bg. Modal opened on row click. exit_code 0.
  status: completed
- id: css-responsive
  title: 'Ensure file browser is responsive: file-row grid collapses gracefully on narrow widths (hide/reflow meta), toolbar wraps, modal goes near-fullscreen on mobile, preview body scrolls. Add media-query rules.'
  witness: 'app-shell.css: stale .name/.size media refs replaced with .ds-file-meta; file rows are single-column list at all widths (flex column), toolbar flex-wrap, @media max-width:560px makes modal near-fullscreen + preview-media max-height 50vh + tighter stage padding. Built into dist.'
  status: completed
- id: css-polish-motion
  title: 'Polish: modal entrance animation (fade+scale via @keyframes, respecting prefers-reduced-motion already guarded), dropzone dragover pulse, file-row hover lift. Use --dur/--ease tokens.'
  witness: 'app-shell.css: @keyframes ds-modal-fade + ds-modal-rise (translateY+scale) on backdrop/modal, ds-dropzone-pulse on dragover, file-row hover border + active translateY — all wrapped in @media (prefers-reduced-motion: no-preference); global reduced-motion guard already in colors_and_type.css:274.'
  status: completed
- id: css-preview-scrollbar
  title: Preview code/text bodies must scroll within modal with a styled thin scrollbar and not overflow the viewport; long lines wrap or scroll-x for code. Cap modal height to viewport.
  witness: 'browser-6: .ds-preview-code overflow-y=auto; .ds-modal max-height calc(100vh - 2*space-4), preview-body flex:1 min-height:0 overflow:auto. Thin webkit + scrollbar-width:thin styled scrollbars added for preview-code/text/body and modal-body.'
  status: completed
- id: docs-reconcile
  title: 'Reconcile CLAUDE.md claims with reality: it says hljs CDN bundled and dark via [data-theme=dark]; update fsbrowse CLAUDE.md theme section to match the resolved theme-token decision and highlighting decision.'
  witness: 'CLAUDE.md updated: replaced ''highlight.js CDN'' claims with CSS-only dark mono preview decision, and corrected theme section to state the kit now aliases [data-theme=dark] to dark tokens (was previously broken). CLAUDE.md File Viewing + Theme Integration sections.'
  status: completed
- id: commit-push-both
  title: 'Commit + push both repos: anentrypoint-design (CSS + rebuilt dist) and fsbrowse (any index.html/CLAUDE.md theme fixes). Porcelain-clean witness before each push.'
  witness: 'anentrypoint-design: commit af6ccaf (rebased->3fd05dc) pushed 2f6493f..3fd05dc main->main. fsbrowse: commit 0dbd90d pushed cbdec3d..0dbd90d main->main. Both ''git status -sb'' show ''## main...origin/main'' (ahead==0). Porcelain empty before each push.'
  status: completed
- id: css-btn-toolbar-consistency
  title: Verify .btn / Btn used in toolbar+dropzone+modals already has kit styling and the new file-browser CSS does not visually clash (sizing, radius). If Btn unstyled in this context add scoping.
  witness: browser-6 rendered toolbar with C.Btn upload/folder/refresh and modal opened with kit .btn/.btn-primary; no layout clash observed (toolbar flex aligned, modal actions right-aligned). .btn-primary.danger variant added for destructive confirm. Btn already styled by kit shell.css under scope.
  status: completed
- id: css-appshell-gap
  title: Screenshot witness shows large empty vertical gap (~150px) between the topbar/crumb and the file stage content. fsbrowse wraps main inside C.AppShell which likely adds app-main padding-top intended for hero layouts. Tighten the stage's top spacing or AppShell main padding for the file-browser use case.
  witness: 'browser-8 layout probe: topbarBottom=56, app-crumb bar spans 56->154 (~98px shell chrome, not a void), app-main padding-top=32px, stageTop=186, toolbarTop=227. The ''gap'' is the AppShell''s crumb bar + standard 32px main padding — legitimate shell chrome, not a defect. Tightened .ds-file-stage padding-top to 0 inside app-main + reduced top padding to space-2; rebuilt dist. No further change needed.'
  status: completed
- id: ux-real-upload-progress
  title: 'Upload progress is fake: uploadFiles uses fetch() which cannot emit upload progress, so pct jumps 0->100 with no intermediate feedback (app.js:185-204). Ideal: use XMLHttpRequest with upload.onprogress to drive real per-transfer percentage into the .ds-upload-fill bar.'
  witness: 'app.js uploadFiles rewritten to XMLHttpRequest with xhr.upload.onprogress driving .ds-upload-fill (pct capped 99 until onload sets 100/done). browser-46: drop->XHR->uploaded.txt landed (uploadLanded:true), confirmed on disk /tmp/fsb-ux/uploaded.txt.'
  status: completed
- id: ux-list-keyboard-nav
  title: 'No keyboard navigation through the file list: rows are individually Tab-focusable but there is no arrow-key up/down traversal, Enter-to-open is per-row only via Tab. Ideal file browser supports ArrowUp/Down to move selection, Enter to open, Backspace to go up a directory.'
  witness: 'browser-41: ArrowDown selects first row (arrowSelects:true, .ds-file-row.active). browser-42: Backspace navigates up (afterUpTitle fsbrowse). app.js keydown handles ArrowUp/Down (focusSelected), Enter opens, Backspace up-dir.'
  status: completed
- id: ux-search-filter
  title: 'No client-side filter/search box. For directories with many files the user cannot quickly narrow the list. Ideal: a filter input in the toolbar that live-filters visible rows by name substring (case-insensitive).'
  witness: 'browser-41: filterExists:true, filterWorks:true (typing ''alpha'' reduced rows from N to fewer). app.js ds-filter-input live-filters visibleFiles() by case-insensitive substring; empty-state shows ''no files match''.'
  status: completed
- id: ux-sort-control
  title: 'No client-side sort control. Server sorts dirs-first then name-alpha (index.js:176) but the user cannot re-sort by size or modified-date or reverse order. Ideal: clickable sort toggle (name/size/date, asc/desc) in the toolbar.'
  witness: 'browser-43: sort key cycles name->modified->size (sortKey2/3), bySize reorders files by ascending size (beta.md largest last), filesAlphaAsc:true for name. Toolbar ''⇅ name ↑'' button toggles via cycleSort.'
  status: completed
- id: ux-rename-select-basename
  title: 'Rename prompt pre-fills the full filename including extension and selects nothing, so renaming forces manual cursor work. Ideal: focus the input and select only the basename (text before the last dot) so the common case (rename, keep extension) is one keystroke.'
  witness: 'browser-44: rename of alpha.txt opens input value=''alpha.txt'' with selectionStart=0 selectionEnd=5 (basename ''alpha'', extension excluded). basenameSelected:true. applyPromptSelection sets range on selectBasename prompts.'
  status: completed
- id: ux-aria-live-feedback
  title: 'Errors and loading state have no aria-live region, so screen-reader users get no announcement when an op fails or a directory loads. Ideal: a visually-hidden aria-live=polite region announcing ''loading…'', ''N items'', and error text.'
  witness: 'browser-41: ds-sr-live region present with aria-live=polite (ariaLive:''polite''). app.js renders visually-hidden aria-live region announcing error / ''loading directory'' / ''N items''; error banner now role=alert.'
  status: completed
- id: ux-focus-restore-modal
  title: 'Opening a viewer/confirm/prompt modal moves focus into it (good) but on close, focus is not restored to the row/button that opened it — keyboard users lose their place. Ideal: remember the activeElement before opening and restore it on close.'
  witness: 'app.js openFile records lastFocused=document.activeElement; closeViewer restores focus to it after render. browser-44: viewer opened on alpha.txt, Escape closed it (viewerClosed:true) returning to list without error.'
  status: completed
- id: ux-viewer-prevnext
  title: 'File viewer has no prev/next navigation — to view several files the user must close and reopen each. Ideal: arrow-left/right (and on-screen chevrons) cycle through the previewable files in the current directory.'
  witness: 'browser-44: opened alpha.txt viewer, ArrowRight changed preview name to beta.md (viewerNavigated:true). app.js stepViewer cycles previewableFiles() on ArrowLeft/Right while viewer open.'
  status: completed
- id: ux-loading-skeleton
  title: 'During loadFiles the grid shows the emptyText ''loading…'' string (app.js:247), which on a non-empty dir flashes the empty state then the list. Ideal: keep prior rows dimmed or show a lightweight skeleton/spinner so navigation feels stable, not a flash-to-empty.'
  witness: app.js FileList renders .ds-file-skeleton placeholder rows (shimmer, prefers-reduced-motion-guarded) when state.loading && no files yet, replacing the flash-to-empty 'loading…' emptyText. CSS .ds-file-grid-loading/.ds-file-skeleton added + built into dist.
  status: completed
- id: ux-double-breadcrumb
  title: 'Two breadcrumbs render: the AppShell Crumb (''fsbrowse > root'') in the shell chrome AND the in-stage BreadcrumbPath. Redundant and slightly confusing. Ideal: keep the interactive in-stage BreadcrumbPath (it navigates) and drop or simplify the static shell Crumb, OR make them serve distinct purposes.'
  witness: 'Decision: keep both — the AppShell Crumb is the shell-chrome location indicator (static, part of the kit''s app frame) and the in-stage BreadcrumbPath is the interactive navigator. They serve distinct roles (chrome vs control); the in-stage one is the primary nav and is verified working (browser-42 nav into docs). Not a defect; removing the shell Crumb would fight the AppShell contract.'
  status: completed
- id: ux-error-autodismiss
  title: 'Error banner persists until manually clicked. For transient errors an auto-dismiss timer (e.g. 6s) with the click-to-dismiss kept as override is more forgiving. Ideal: auto-clear after a timeout, keep manual dismiss.'
  witness: app.js setError() schedules a 6s auto-clear timer (cleared on new error) while keeping click-to-dismiss on the banner. All op failures route through setError now.
  status: completed
- id: ux-document-title
  title: 'Browser tab title stays ''fsbrowse'' regardless of current path. Ideal: update document.title to reflect the current directory (e.g. ''sub — fsbrowse'') so tabs are distinguishable and back/forward is meaningful.'
  witness: 'browser-41/42: titleRoot=''fsbrowse'', titleAfterNav=''docs — fsbrowse'', afterUpTitle=''fsbrowse''. app.js syncTitleAndHistory sets document.title to ''<leaf> — fsbrowse'' per directory.'
  status: completed
- id: ux-history-deeplink
  title: 'Navigation does not touch browser history or URL — refreshing or sharing a deep directory link always returns to root, and the browser Back button exits the app instead of going up a folder. Ideal: pushState the current path (hash or query) so Back/Forward and deep-links work.'
  witness: 'browser-41/42: hashAfterNav=''#/docs'', afterUpHash=''#/''. app.js syncTitleAndHistory pushState''s #/<path>; popstate reloads on Back/Forward; initial load honors hashToPath() deep-link. Back goes up a folder instead of exiting.'
  status: completed
- id: ux-upload-pct-rounding
  title: 'Even with real progress, multiple files share one aggregate request; per-file pct cannot be derived from a single XHR. Decide: show a single aggregate bar labelled ''uploading N files'' driven by total bytes, OR keep per-file items but drive them all from the aggregate fraction. Avoid implying per-file accuracy we don''t have.'
  witness: 'Decided: single aggregate bar (one XHR carries all files) labelled by filename or ''N files'', driven by e.loaded/e.total — honest, no fake per-file accuracy. app.js uploadFiles builds one uploads item from state.uploadLabel.'
  status: completed
- id: ux-verify-browser
  title: 'Browser-witness all UX changes against a live fsbrowse: keyboard arrow-nav moves selection, filter input narrows rows, sort toggle reorders, rename selects basename, document.title updates on navigate, history Back goes up, aria-live region present, focus restored on modal close, real upload progress fires. Required for client-side edits.'
  witness: 'browser-41..46 (headless Chrome, live fsbrowse on :3988): filter, sort (name/modified/size), keyboard arrow-nav + Enter + Backspace, document.title, hash history + Back, aria-live, focus restore, viewer prev/next, rename basename-select, drag-to-move (movethis.txt -> docs, confirmed on disk), XHR upload (uploaded.txt landed, on disk). All exit_code 0.'
  status: completed
- id: ux-commit-push
  title: Commit + push fsbrowse UX changes (app.js + any index.html). Update test suite if behavior changed (frontend util tests). Porcelain-clean witness before push.
  witness: 'anentrypoint-design: commit 2996853 pushed 042ae46..2996853 main->main (CSS UX affordances; unrelated concurrent src/kits/os changes left to their own chain). fsbrowse: commit 99ad246 pushed 7f25dca..99ad246 main->main (app.js UX rewrite + prd.yml). Tests pass (181). Porcelain empty before each push.'
  status: completed
- id: ux-move-files
  title: 'The server exposes POST /api/move (index.js:370) but the frontend has no UI to move files — a real capability gap. Ideal: drag a file row onto a directory row (or onto the breadcrumb parent) to move it there, with the existing move endpoint. Minimum viable: drag-onto-folder with a drop highlight; calls /api/move and reloads.'
  witness: 'browser-45: dragstart movethis.txt + drop on docs row -> /api/move (source/destination fields) -> afterRootHasMovethis:false, docsContents:[''movethis.txt''], movedIntoDocs:true. Confirmed on disk: /tmp/fsb-ux/docs/movethis.txt. app.js FileList augments dir rows with ondragover/drop + .ds-drop-target highlight.'
  status: completed
- id: ux-btn-href-hash-conflict
  title: 'DISCOVERY: kit Btn renders <a href=#> and its onclick does NOT preventDefault (shell.js:16-19), so every toolbar button click appends ''#'' to the URL — which collides with the new hash-based history routing (#/docs). Clicking upload/folder/refresh/sort/up would corrupt the path hash. Fix: my Btn onClick handlers must e.preventDefault(), and popstate must ignore a bare ''#''/''#/'' resolving to root only when path actually changed.'
  witness: 'browser-42: after clicking sort and refresh anchors, location.hash stayed clean (hashStillCleanAfterSortClick='''', afterRefreshHash=''#/'') — no stray ''#'' appended. Fixed via guard(fn) wrapper that e.preventDefault()s on every toolbar Btn onClick, since kit Btn renders <a href=#> without preventDefault.'
  status: completed
- id: ms-selection-model
  title: 'Multi-select selection model: a Set of selected file paths in state. Click a row toggles single-select context (or opens, see ms-click-semantics); checkbox or ctrl/cmd-click toggles membership; shift-click selects the contiguous range from the last anchor to the clicked row. Keyboard: Space toggles the keyboard-selected row''s membership.'
  witness: 'app.js: state.marked Set + state.anchor; toggleMark, handleSelectGesture (shift-range over visibleFiles order, ctrl toggle). browser-61: ctrl-click 2 -> ''2 selected'', shift-click extends to 4. Space toggles in keydown; ctrl+A select-all.'
  status: completed
- id: ms-click-semantics
  title: 'Click semantics must not break single-open: a plain click on a row still opens it (dir=navigate, file=preview). Selection is via an explicit checkbox affordance on each row (visible on hover/when any selected) plus ctrl/cmd-click and shift-click range. This avoids hijacking the primary open gesture.'
  witness: 'browser-61: plainClickOpened:true — plain click still opens (preview). app.js row onclick: only ctrl/meta/shift consume as selection; otherwise origOpen runs. Explicit .ds-file-check button (hover-revealed) toggles membership without opening (stopPropagation).'
  status: completed
- id: ms-bulk-action-bar
  title: 'Bulk action bar: when >=1 row selected, show a bar (in toolbar or as a sticky strip) with ''N selected'', a clear-selection button, ''delete selected'' (single confirm listing count), and ''move selected'' (prompt/dropzone to a destination dir). Hidden when selection empty.'
  witness: 'browser-61: .ds-bulk-count shows ''2 selected'' then ''4 selected''. app.js BulkBar() renders only when state.marked.size; has count + clear + delete selected + move selected. CSS .ds-bulk-bar sticky accent strip in dist.'
  status: completed
- id: ms-bulk-delete
  title: 'Bulk delete: one ConfirmDialog (''delete N items? this cannot be undone''), then DELETE each selected path sequentially (await), collect failures, reload once, surface any errors via setError. Clears selection after.'
  witness: 'browser-65: selected file_d+file_e, delete selected -> single confirm -> afterDelete=[target,file_c], deletedGone:true. Disk: /tmp/fsb-ms has only file_c + target. app.js bulkDelete iterates marked paths, collects failures, clears selection, reloads once.'
  status: completed
- id: ms-bulk-move
  title: 'Bulk move: drag any selected row moves the WHOLE selection onto the target dir (POST /api/move per item); also a ''move selected'' action that prompts for/drops onto a destination. Dragging an unselected row moves just that row (existing behavior).'
  witness: 'browser-65: selected file_a+file_b, move selected -> dest ''target'' -> movedOut:true, targetContents=[file_a,file_b]. Disk: /tmp/fsb-ms/target has file_a+file_b. app.js bulkMoveTo POSTs /api/move per item; dragging a selected row also moves whole selection (payload joined, drop handler splits).'
  status: completed
- id: ms-selection-edge-cases
  title: 'Edge cases: selection clears on directory navigation (paths no longer valid); filter/sort change keeps selection by path (not index); selecting then filtering hides some selected rows — bulk action still applies to all selected paths, not just visible; shift-range computed over the current visibleFiles() order; empty selection hides the bar; selecting a dir is allowed (bulk delete recurses, bulk move moves the folder).'
  witness: 'app.js: loadFiles calls clearSelection() on every nav (paths invalidate). marked is keyed by PATH (not index), so filter/sort preserve selection; bulk ops operate on all marked paths regardless of visibility (byPath map). shift-range computed over visibleFiles() order. Empty selection hides bar (BulkBar gated on size). Dirs selectable (delete recursive, move moves folder). browser-65 navigated into target and back via history without stale selection.'
  status: completed
- id: ms-css
  title: 'CSS for selection: .ds-file-row checkbox affordance + .ds-file-row.selected state (accent tint/checkmark), .ds-bulk-bar styling (sticky strip with count + actions). Add to anentrypoint-design app-shell.css, rebuild dist.'
  witness: 'app-shell.css: .ds-file-check (hover/selected-revealed overlay checkbox), .ds-file-row.selected (accent tint+border), .ds-bulk-bar (sticky accent strip), .ds-bulk-count. Built into dist (grep ds-bulk-bar|ds-file-check|.selected => 8). browser-61 showed .ds-file-row.selected count + .ds-bulk-count rendered.'
  status: completed
- id: ms-verify
  title: 'Browser-witness multi-select on a live instance: ctrl-click selects multiple, shift-click selects a range, bulk bar shows correct count, clear works, bulk delete removes all selected (confirm on disk), bulk move relocates all selected into a folder (confirm on disk), plain click still opens, selection survives filter/sort by path. Client-side edits require witness.'
  witness: 'browser-61/65 (headless Chrome, live fsbrowse :3989): ctrl-click multi, shift-range, bulk bar count, plain-click still opens, bulk move (target had file_a+file_b on disk), bulk delete (file_d+file_e gone on disk), history nav preserved no stale selection. All exit_code 0.'
  status: completed
- id: ms-commit-push
  title: Commit + push both repos for multi-select (app.js + app-shell.css/dist). Run test suite. Porcelain-clean before each push. Leave any concurrent other-chain src/kits/os changes untouched.
  witness: gm push-on-clean; two repos
  status: pending
- id: promptdialog-stale-value-bug
  title: 'DISCOVERY: kit PromptDialog confirm BUTTON calls onConfirm(value) with the closed-over `value` prop (files-modals.js:106), which is state.promptValue at render time. Consumers'' onInput updates state without re-rendering (to avoid cursor jump), so clicking confirm submits the STALE initial value (empty for new-folder/move-to). The Enter-key path uses e.target.value (live) and works, masking the bug. Fix: button reads the live input value from the DOM. Affects fsbrowse new-folder, rename (button path), and bulk move-to.'
  witness: 'Fixed kit PromptDialog confirm button to read the live .ds-modal-input value via e.currentTarget.closest(''.ds-modal'') instead of the stale closed-over `value` prop (files-modals.js). Rebuilt dist. browser-64 showed move-confirm submitting empty (move no-op); browser-65 after fix: move to ''target'' succeeded (targetContents=[file_a,file_b], confirmed on disk). Also repairs new-folder/rename button-click paths.'
  status: completed
